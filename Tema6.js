/*
    TEMA 6: ESTRUTURAS CONDICIONAIS E REPETIÇÃO
    
    METODOS ARRAYS PARA LEMBRAR
        .push() -> adiciona item no final 
        .pop() -> remove o ultimo item
        .indexOf('item') -> Encontra a posição de um item
        .includes() -> Veirifica se um item existe se não encontra retornar -1
        .splice(inicio, quantos, itens) -> serve para remover ou susbstituir
                 ou adicionar elementos em qualquer posição do array.
        .slice(inicio, fim) -> Retorna uma copia de parte do array sem modificar o original
        .join(separador) -> Junta todos os itens em uma única string, separados por um caractere
        .reverse() -> Inverte a ordem dos elementos no próprio array
        .sort() -> Ordena os itens. Por padrão ordena como string(alfabética)
                para numeros utilize sort((a,b) => a - b)
    METODOS OBJETOS PARA LEMBRAR 
        .delete objeto.chave -> deleta uma propriedade
*/
